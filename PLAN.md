# 「小引导」完整产品计划

> 面向**认知障碍人士（培智学生 / 自闭症）、银发老人、数字新手**的数字生活仿真训练 App。
> AI 角色把任何"抽象任务"（挂号、取快递）拆成"一个动作一步"的具象引导，在**仿真界面**里练习，点错无后果。
> 全部本地运行，少见界面靠**语音描述现场生成**。

---

## 一、三类人群的共性与差异（产品设计的根）

### 共性（同一套引擎能吃下的原因）

| 表层问题 | 底层根因 | 统一解法 |
|---|---|---|
| 不敢点、怕点错 | 对未知的恐惧 + 不可逆后果焦虑 | 仿真沙盒，点错零代价 |
| 看不懂界面 | 抽象符号（图标/术语）无法理解 | 具象化拆解：颜色+形状+位置描述，禁用术语 |
| 教了就忘 | 工作记忆弱 / 注意涣散 | 单步引导 + 即时反馈 + 可无限重播 |
| 没人耐心教 | 真人教学成本高、语气不可控 | AI 角色永远温柔、永远耐心、随时可练 |

### 差异（必须分开处理的参数）

| 维度 | 培智学生/自闭症 | 老人（无认知障碍） | 设计应对 |
|---|---|---|---|
| 核心障碍 | 抽象思维弱、注意力易分散 | 眼花、手抖、术语不懂、怕"扣钱" | **学习画像**（下详） |
| 话术风格 | 童声、短句≤12字、夸赞高频 | 尊重平等、解释"为什么"、不哄小孩 | 话术 persona 可切换 |
| 操作难点 | 分不清哪里能点 | 手抖误触、看不清小字 | 热点容错区、字号放大 |
| 高频诉求 | 基础技能学习 | **解决当下真实问题**：字体太小、声音没了、垃圾短信、连 WiFi | 新增"**急救箱**"模块 |
| 奖励机制 | 撒花/奖章/角色鼓掌有效 | 觉得幼稚，需要"学会了能自己用"的实用感 | 奖励表现随 persona 变化 |

> **关键洞察：老人线最大的差异化是"急救箱"** —— 老人遇到的真实问题（手机突然没声音、字体看不清、误开飞行模式）往往不是"学新技能"，而是"排除突发故障"。这是培智场景库之外要新增的一整个内容域。

### 学习画像（首次启动选择，全引擎参数随之调整）

```json
"profile": {
  "persona": "child | senior",        // 话术语气、角色形象、奖励风格
  "font_scale": 1.0,                   // 老人默认 1.4
  "speak_rate": 0.9,                   // 语速，老人/培智都偏慢但原因不同
  "tap_tolerance": 24,                 // 热点容错像素，老人默认加大（手抖）
  "auto_replay": true,                 // 是否自动重播演示
  "explain_why": false                 // 老人模式 true：多一句"为什么这样做"
}
```
热点容错像素只适用于逻辑部分，老人练习中要用普通的，因为软件没有适老化
**引擎只有一套，画像决定参数** —— 这是能用一套代码服务三类人群的关键架构决策。

---

## 二、设计原则（不可违背）

1. **零问答、零阅读负担** —— 全程语音 + 动画演示 + 表情反馈，不要求学员打字或长听
2. **单步引导** —— 任何时刻屏幕上只有一个任务："找到这个按钮，点它"
3. **一个动作一步** —— 话术里出现"然后""接着"就是拆得不够细（拆分红线）
4. **仿真沙盒** —— 训练环境是假的，点错不会真的付款/拨号，消除对未知的恐惧
5. **错误无惩罚** —— 点错 → 角色温柔重播演示；点对 → 即时夸奖动画

---

## 三、学习路径：事物 → 图标 → 软件 → 生活（五级课程）

| 级 | 名称 | 学什么 | 界面来源 | 适用人群 | 优先级 |
|---|---|---|---|---|---|
| **L0** | 事物认知 | 认识实物及其功能：手机、银行卡、现金、二维码、快递柜、闸机 —— "这个铁柜子是取快递的" | AI 生成实物图 / 照片 | 认知障碍 | P1 |
| **L1** | 图标认知 | 实物 ↔ 图标对应：真电话 ↔ 绿色听筒图标；单屏找图标 | 预制裁剪桌面图 | 全人群 | P0 |
| **L2** | 软件操作 | 单界面找按钮 → 多界面走流程 | 真实截图 + 热点 | 全人群 | P0 |
| **L3** | 生活场景 | 公共设施 + 真实生活流程串联（含 L0 实物环节混排） | 语音生成少见界面 | 全人群 | P1 |
| **L4** | 急救箱 | **真实故障排查**：字体太小、手机没声音、连不上 WiFi、误开飞行模式、识别诈骗 —— 在**真实系统设置**里引导，改的是真手机 | 真实系统界面 + 实时截图标注 | **老人为主** | **P0（老人线核心）** |

> **L0→L1 的"对应课"是灵魂**：先认真实听筒，再认绿色听筒图标 —— 这一步跨越就是"具象→抽象"的教学本身。
>
> **L4 是老人线的分水岭**：培智学生练"学会"，老人要"解决"。L4 直接在真实系统设置里引导（"点这个齿轮 → 点'显示' → 把字体拖大"），学完即见效 —— 这是老人愿意用下去的理由。
> **风险与应对**：真实设置可能被改乱 → 每个 L4 场景配"**一键恢复**"引导（"我们把它调回去"），且涉及删除/支付/关机等不可逆操作前必须二次确认。
> **LLM 实时兜底**：老人手机型号千差万别，预制场景覆盖不全 → 学员/家属可**拍照当前界面**，LLM 实时识别控件并生成引导步骤（30 秒审核后使用），实现"任何手机都能教"。

---

## 四、场景库规划（生活域 × 人群 × 优先级）

| 生活域 | 场景 | 适用人群 | 级别 | 优先级 |
|---|---|---|---|---|
| 通讯 | 打电话（拨号/接/挂） | 全人群 | L2 | **P0** |
| 通讯 | 微信发语音消息（含长按） | 全人群 | L2 | P0 |
| 通讯 | 视频通话 | 全人群 | L2 | P1 |
| 基础操作 | 解锁、调音量、手电筒、拍照 | 全人群 | L1/L2 | **P0** |
| 图标关 | 认电话/微信/相机/设置图标 | 全人群 | L1 | **P0** |
| **急救箱** | 字体太小看不清 | **老人** | L4 | **P0** |
| **急救箱** | 手机没声音 / 通话声音小 | **老人** | L4 | **P0** |
| **急救箱** | 连不上 WiFi / 不知道连哪个 | **老人** | L4 | **P0** |
| **急救箱** | 识别诈骗电话 / 假冒链接 / 虚假中奖 | **老人**（安全刚需） | L4 | **P0** |
| **急救箱** | 误触飞行模式没信号 | **老人** | L4 | P1 |
| **急救箱** | 手机卡顿 / 垃圾清理 | **老人** | L4 | P1 |
| **急救箱** | 误删 App 怎么找回 | **老人** | L4 | P2 |
| 支付 | 出示付款码、扫二维码 | 老人 / 数字新手 | L2 | P1 |
| 出行 | 公交乘车码、地图查路线 | 老人 / 数字新手 | L2 | P1 |
| 就医 | 手机挂号 | 老人 / 数字新手 | L2 | P1 |
| 生活 | 外卖点餐、查快递、设闹钟、看天气 | 全人群 | L2 | P1 |
| 公共设施 | 快递柜取件、ATM 取款、售票机买票、医院自助挂号机、超市自助结账、地铁闸机 | 老人 / 数字新手 | L3 | **P1（语音生成的主战场）** |

预制原则：**高频场景手工打磨**（截图真实、话术逐字审），**低频长尾全部交给语音生成器**。
人群适配原则：**同一场景存 child / senior 两套话术**，引擎按画像自动选用。

---

## 五、核心引擎（数据驱动，全本地）

### 5.1 DSL：三级结构 + 6 原子动作（P0）

课程 → 环节（phase）→ 微步骤（step），每步只允许一个动作：

| 原子动作 | 用途 | 示例话术 |
|---|---|---|
| `observe` | 只看不动手，建立画面认知 | "这是快递柜的屏幕" |
| `tap` | 点一下 | "点绿色听筒" |
| `long_press` | 按住（难点动作，单独教） | "按住说话按钮，别松手" |
| `swipe` | 滑动 | "手指从下往上滑" |
| `type_char` | **输一个字/一个数字就算一步** | "找到 3，点它" |
| `wait` | 学会等待（真实刚需） | "屏幕转圈圈，我们等它" |

> 注意 `observe` 和 `wait` 也是正式步骤 —— 培智教学里"认出画面"和"忍住不点"本身就是要训练的能力。

### 5.2 每步自带辅助阶梯（P0，数据自带，非代码逻辑）

```
10秒无操作 → 重说一遍 + 目标闪烁
错 1 次   → 箭头指向 + 其余变暗
错 2 次   → 小手动画演示一次，再让学员点
错 3 次   → 自动完成，标记"待复习"，角色说"这次我帮你"
```

（特殊教育 least-to-most prompting，引擎按表执行即可）

### 5.3 双模屏幕渲染（P0）

```json
"screen": {"type": "image",     "src": "keypad.jpg"}                  // 真实截图 + 隐形热点
"screen": {"type": "synthetic", "style": "kiosk", "elements": [...]}  // 程序绘制生成界面
```

- **image 模式**：真实 App 截图铺满 + AI 标注的热点坐标（预制场景用）
- **synthetic 模式**：元素树（按钮/文字/数字键盘/指示灯）由 Flutter 直接绘制，热点坐标**由构造保证精确** —— 生成少见界面的主力，不依赖生图质量

### 5.4 引导状态机 + AI 角色（P0）

```
开场打招呼 → 演示（小手动画） → "轮到你了" → 等待操作
   ↓点对          ↓点错            ↓超时
 夸奖+下一步    辅助阶梯          辅助阶梯
```

角色 = AI 生图立绘 + 3 张表情差分（微笑/说话/鼓掌）+ 嘴型开合两张循环，配 TTS。

---

### 5.5 角色与话术：persona 双轨制（P0）

同一引导引擎，两套"人格"：

| 维度 | child 版 | senior 版 |
|---|---|---|
| 角色形象 | 卡通小伙伴（小动物/小机器人） | 温和的邻家晚辈/社区志愿者形象 |
| 话术风格 | "哇！你找到啦！太棒了！" | "这个字有点小，我们把它调大，以后看着就不费眼了" |
| 夸奖方式 | 撒花+动画音效 | 一句"好了，现在清楚多了"，克制不肉麻 |
| 出错安抚 | "没关系，我们再试一次" | "这个按钮藏得是有点深，不怪您" |

话术存在场景 JSON 的 `say.child` / `say.senior` 双字段，引擎按画像取用 —— **场景只录一份操作流，话术各写各的**。

## 六、语音需求 → 少见界面生成器（P1，核心差异化）

> 家长/老师用嘴说需求（"教他取快递"），系统自动生成这个少见界面的仿真场景。

### 6.1 流程（App 内，家长/老师侧入口）

```
🎤 按住说话："我想教他一个人在快递柜取快递"
  ↓ ASR（P1 用 Android 系统 SpeechRecognizer；P2 换 sherpa-onnx 离线模型）
  ↓ LLM 任务分析：拆环节链
     [走到柜子前→看屏幕→点"取快递"→输取件码→柜门弹开→取件→关门]
  ↓ 每个环节产出：
     ① synthetic 界面（元素树：选 style 模板 kiosk/atm/app + 布局参数）
     ② 细拆微步骤（6 原子动作 + say 话术 ≤12字 + 辅助阶梯）
     ③ observe 步骤可配 AI 生图实物照（P2，"这是快递柜，认准它"）
  ↓ 本地校验：schema 合法性 + 拆分红线检查（连接词检测）+ 坐标越界检查
     不合格 → 自动重试 1 次 → 仍不合格降级为"单步找按钮"保底
  ↓ 老师预览试玩 → 一键入库（JSON 存本地，永久离线可用）
```

### 6.2 质量保障三道闸

1. **Prompt 约束**：动作枚举封闭、单动作强制、话术字数限制、给 2 个 few-shot 范例
2. **程序校验**：JSON Schema + 规则检查器（不依赖 LLM 自觉）
3. **人工确认**：老师预览页是必经环节，未确认不入库 —— 特殊教育的安全红线

### 6.3 界面风格模板库（P1）

预置参数化皮肤：`kiosk`（快递柜/售票机大屏）、`atm`、`pos`（自助结账）、`app_light/app_dark`（手机界面）。
LLM 只选模板 + 填内容，不自由发挥布局 —— **可控性 > 花哨**。

### 6.4 双 persona 话术生成（P1）

生成场景时 LLM 同时输出 `say.child` / `say.senior` 两套话术；预制场景由人工逐字审双版本。

### 6.5 学员侧实时生成：LLM 作为主力引擎（P1）

**预制场景保准确性，LLM 实时生成保覆盖率** —— 学员遇到任何少见界面/突发问题，可现场生成：

| 触发方式 | 输入 | LLM 输出 | 审核 |
|---|---|---|---|
| **拍照识别** | 学员/家属拍当前手机屏幕 | 识别控件树 → 生成引导步骤链 | 家长/老师 30 秒确认 |
| **语音描述** | "这个手机怎么调大字体" | 联网搜索该型号步骤 → 生成引导 | 同上 |
| **选项分支** | 学员在引导中点"我想先学别的" | 实时生成新场景入口 | 零审核（已有场景跳转） |

**技术实现**：LLM 输出必须符合 5.1 DSL schema，引擎按数据渲染，不解析自由文本。

---

## 七、技术栈（全部免训练）

| 模块 | 选型 | 优先级 |
|---|---|---|
| App | Flutter（Android 首发） | P0 |
| 界面渲染 | Flutter CustomPainter（遮罩挖孔 + 热点 + synthetic 绘制） | P0 |
| 角色 | AI 生图差分 + 简单帧动画 | P0 |
| TTS | 系统 TTS / edge-tts（child persona 用童声，senior persona 用温和成年声） | P0 |
| 场景生成 | Claude API（多模态：截图解析 + 文本规划都用它） | P0 |
| ASR | Android SpeechRecognizer | P1 |
| **L4 实时界面标注** | **Android 无障碍服务（AccessibilityService）读取真实界面控件树 → 悬浮窗画高亮** | **P1（P0 先用截图引导代替）** |
| 存储 | 本地 JSON 文件（assets 内置 + 应用目录生成区） | P0 |
| 生图（实物照/装饰） | 即梦/SD API | P2 |
| 离线 ASR | sherpa-onnx | P2 |
| 情感 TTS | 讯飞中文童声 | P2 |
| Live2D 角色 | 替换差分图方案 | P2 |

---

## 八、进度与复习（P1）

- 本地学习档案：每步记录 `{首次成功率, 触发辅助层级, 用时}`
- 掌握度 = 连续 3 次无辅助通过 → 场景标记"已掌握"
- 家长页：纯文字报告 —— "今天练了取快递，'输取件码'还需要帮助"
- **家属页（老人线）**：子女远程收到"爸爸今天自己学会了连 WiFi" —— 情感激励 + 家属安心
- 错题场景自动在下次启动时优先复习

---

## 九、优先级总表

| 优先级 | 内容 |
|---|---|
| **P0** | DSL + 状态机 + 辅助阶梯、双模渲染、L1 图标关×5、L2 预制场景×8（通讯/基础操作）、**L4 急救箱场景×4（老人线）**、学习画像（persona 双轨）、AI 角色 + TTS、截图→场景生成器（人工输入截图版）、本地存储 |
| **P1** | **语音需求→少见界面生成器**、synthetic 模板库×5、L0 事物对应课、L3 公共设施场景×6、家长/家属报告、复习调度、**学员侧实时生成（拍照/语音→LLM 现场生成引导）** |
| **P2** | 生图实物照、离线 ASR、Live2D、情感 TTS、安全防诈进阶课、平板大屏适配 |

---

## 十、明确不做清单

1. **不训练任何模型** —— 只用现成 API 和预训练模型（LLM 推理 + 联网搜索，不微调不训练）
2. **不做摄像头/视线/表情识别**（本轮）
3. **不做后端、账号、云同步** —— 纯单机，生成结果本地存；家属通知走系统分享，不建服务端
4. **学员侧互动 = 选项按钮 + 语音应答 + 点哪里问哪里** —— 不做聊天框，但提供结构化选择（"你想先学哪个？"）和语音意图识别（"准备好了" / "我不明白" / "这是什么"）；LLM 实时兜底解释，**主要引导步骤也可实时生成**（见下）
5. **不做真实支付/真实拨号** —— 全部仿真，仿真支付界面带"练习模式"视觉标记防混淆
6. **LLM 是核心引擎，不是辅助工具** —— 预制场景保准确性，LLM 实时生成保**覆盖率**；学员遇到任何少见界面/突发问题，可实时拍照/描述 → LLM 现场生成引导步骤 → 家长/老师侧 30 秒审核 → 立即使用；同时支持**联网搜索**补充知识（"北京现在怎么挂号"）
7. **不做 UGC 社区/场景分享市场**
8. **不做 iOS**（首发仅 Android）
9. **不做多语言**（仅中文，话术为中文教学语气优化）
10. **不做重度游戏化**（星星/奖章即时反馈即可，不做养成/排行榜 —— 避免过度刺激；老人版默认关闭奖章动画）

---

## 十一、里程碑（按依赖排序，不估时）

1. **M1 引擎**：DSL 定稿 → 渲染器 + 状态机 + 辅助阶梯 → 手搓 3 个场景跑通
2. **M2 内容**：L1/L2 预制场景铺满 P0 清单 + **L4 急救箱老人场景×4** + 角色/TTS 体验闭环
3. **M3 生成器**：截图版先生成 → 语音输入版 → synthetic 模板库 → 双 persona 话术生成
4. **M4 课程**：L0 对应课 + L3 公共设施 + 复习调度
5. **M5 试点**：双线试点 —— 1 所培智学校/机构 + 1 个社区老人手机班，只改参数（语速、辅助时机、按钮大小、persona 话术）不加功能

---

## 十二、待拍板事项

| # | 问题 | 选项 | 影响 |
|---|---|---|---|
| 1 | 生成器 LLM 选型 | 国内可直连（GLM） | 网络稳定性，试点学校环境可能受限 |
| 2 | L0 事物图片风格 | AI 卡通 / 真实照片质感 | 教学泛化效果，特教界一般建议真实照片起步 |
| 3 | L4 急救箱的实现深度 | 纯截图引导（P0，简单）/ 无障碍服务实时标注真实界面（P1，体验好但开发量大） | 老人线核心体验的技术路线 |
